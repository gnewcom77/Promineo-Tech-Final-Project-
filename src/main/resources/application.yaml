spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pepper_boss
    username: pepperboss
    password: pepperboss

  jpa:
    hibernate:
      ddl-auto: update   
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true 
    
  sql:
     init:
        mode: always